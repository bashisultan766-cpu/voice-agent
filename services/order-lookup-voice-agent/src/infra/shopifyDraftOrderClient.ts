/** Narrow boundary for Shopify draft-order writes. */
import { createShopifyDraftOrder as createDraftOrder } from "../adapters/shopifyStorefrontAdapter.js";

export const createShopifyDraftOrder = createDraftOrder;

Phone Number: (845) 675-7505

VOICE CONFIG
A call comes in:
Type: Webhook
URL: https://api.us.elevenlabs.io/twilio/inbound_call
Method: HTTP POST

Primary handler fails:
Type: Webhook
URL:
Method: HTTP POST

Call status changes:
Type:
URL:
Method: HTTP POST

MESSAGE CONFIG
A message comes in:
Type: Webhook
URL: https://demo.twilio.com/welcome/sms/reply/
Method: HTTP POST

Region:
United States (US1)

Notes:
Current inbound voice is pointed to ElevenLabs native Twilio integration.

--- Cutover (Voice Agent Platform / this repo) ---
If calls must be answered by THIS app (Twilio Gather + OpenAI, optional ElevenLabs TTS):
1) Twilio Console → Phone Numbers → (845) 675-7505 → Voice & Fax:
   - A call comes in: Webhook POST → https://<YOUR_PUBLIC_API_DOMAIN>/api/twilio/voice/inbound
   - Call status changes (optional): POST → https://<YOUR_PUBLIC_API_DOMAIN>/api/twilio/voice/status
2) Remove or replace the ElevenLabs URL above; two different owners cannot both handle the same inbound webhook.
3) In the dashboard/database: agent status ACTIVE; map number as +18456757505 on Agent.twilioPhoneNumber or an ACTIVE PhoneNumber row (see docs/TWILIO-LIVE-CALL-CHECKLIST.md).
4) Production: PUBLIC_WEBHOOK_BASE_URL must exactly match the public API origin (Twilio signature validation).